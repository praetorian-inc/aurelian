## aurelian gcp recon graph

Build GCP IAM graph using the hierarchy processor.

```
aurelian gcp recon graph [flags]
```

### Options

```
  -h, --help                 help for graph
      --indent int           the number of spaces to use for the JSON indentation
      --module-name string   the name of the module for dynamic file naming
      --outfile string       the default file to write the JSON to (can be changed at runtime) (default "out.json")
  -o, --output string        output directory (default "aurelian-output")
```

### SEE ALSO

* [aurelian gcp recon](aurelian_gcp_recon.md)	 - recon commands for gcp

###### Auto generated by spf13/cobra
