## aurelian aws recon scan-amis

Scans Amazon Linux AMIs for OS-level vulnerabilities by invoking Caligula's --mod ami pipeline. Caligula peels the AMI's EBS snapshot, extracts the RPM database, and matches against Amazon Linux Advisory (ALAS) feeds with EPSS and CISA KEV enrichment. No instance launch required — reads the AMI's EBS snapshot directly via the EBS Direct API.

```
aurelian aws recon scan-amis [flags]
```

### Options

```
  -i, --ami-ids strings       Specific AMI IDs to scan (default: all owned AMIs)
      --caligula-cmd string   Path to caligula binary (default "caligula")
  -h, --help                  help for scan-amis
      --kev-only              Only report KEV-flagged vulnerabilities
      --opsec_level string    Operational security level for AWS operations (default "none")
      --output-dir string     Base output directory (default "aurelian-output")
  -p, --profile string        AWS profile to use
      --profile-dir string    Set to override the default AWS profile directory
  -r, --regions strings       AWS regions to scan (default [us-east-1])
```

### SEE ALSO

* [aurelian aws recon](aurelian_aws_recon.md)	 - recon commands for aws

###### Auto generated by spf13/cobra
