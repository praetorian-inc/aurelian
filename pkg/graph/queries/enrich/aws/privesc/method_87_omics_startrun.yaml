id: aws/enrich/privesc/method_87
name: IAM PassRole + HealthOmics CreateWorkflow + StartRun Privilege Escalation
platform: aws
type: enrich
category: privesc
description: |
  Detects principals with iam:PassRole, omics:CreateWorkflow, and omics:StartRun that
  can create an AWS HealthOmics WDL workflow with an admin execution role and start a
  run whose task exfiltrates credentials to S3. Both create and start permissions
  are required to complete the attack path.
severity: high
order: 187
cypher: |
  MATCH (attacker:Principal)-[passrole]->(target)
  WHERE type(passrole) = 'IAM_PASSROLE'
  WITH attacker
  MATCH (attacker)-[create]->(svc)
  WHERE type(create) = 'OMICS_CREATEWORKFLOW'
  WITH attacker, svc
  MATCH (attacker)-[run]->(svc)
  WHERE type(run) = 'OMICS_STARTRUN'
  MERGE (attacker)-[pe:CAN_PRIVESC]->(svc)
  ON CREATE SET pe._created = timestamp()
  SET pe.method = 'iam:PassRole + omics:CreateWorkflow + omics:StartRun',
      pe.severity = 'high',
      pe._updated = timestamp()
  RETURN count(pe) as edges_created
