id: aws/enrich/privesc/method_29
name: Glue UpdateDevEndpoint Privilege Escalation
platform: aws
type: enrich
category: privesc
description: |
  Detects principals with glue:UpdateDevEndpoint permission that can modify
  Glue development endpoints to execute arbitrary code with the endpoint's IAM role.
severity: medium
order: 129
cypher: |
  MATCH (attacker:Principal)-[perm]->(target)
  WHERE type(perm) = 'GLUE_UPDATEDEVENDPOINT'
  AND coalesce(target.Arn, target.arn) <> coalesce(attacker.Arn, attacker.arn)
  MERGE (attacker)-[pe:CAN_PRIVESC]->(target)
  ON CREATE SET pe._created = timestamp()
  SET pe.method = 'glue:UpdateDevEndpoint',
      pe.severity = 'medium',
      pe._updated = timestamp()
  RETURN count(pe) as edges_created
